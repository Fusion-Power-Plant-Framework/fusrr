name: FusRR - publish

on:
  release:
    types: [published]

permissions:
  contents: read

jobs:
  docker:
    runs-on: ubuntu-latest

    # environment: release
    permissions:
      contents: read
      packages: write
      attestations: write
      id-token: write

    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
        with:
          fetch-depth: 0
      - name: Push FusRR Docker image
        uses: ./.github/workflows/actions/push-docker-image-ghcr
        with:
          image_name: "fusrr"
