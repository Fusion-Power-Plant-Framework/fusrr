"""test for python_entry"""
import os
from pathlib import Path

import renderingpipeline.blender_tools as bt

imagepathname = "render.png"


def test_save_image():
    """Test to assert if render has been saved"""
    delete_png = Path.cwd().glob(
        "*.png"
    )  # will delete all pngs in cwd to ensure it is checking for the one generated by Python_entry
    for path in delete_png:
        delete_png = path
        os.remove(delete_png)

    bt.save_image(imagepathname)
    assert Path(imagepathname).exists()
